import styled from 'styled-components';
import mixins from '@styles/mixins';

export const ShapesCSS = styled.div`
  ${mixins.infoContainer}
  position: absolute;
  top: 0;
  left: -190px;
  height: 200px;
  overflow-x: auto;
`;

export const TitleCSS = styled.h3`
  text-align: center;
`
export const ShapeContainerCSS = styled.div`
  margin-top: 1rem;
`
