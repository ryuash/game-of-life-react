import { css } from 'styled-components';

const mixins: any = {
  infoContainer: css`
    padding: 1rem;
    width: 50%;
    min-height: 50px;
    border: dashed 2px #d4d1d1;
    background: white;
    max-width: 200px;
    border-radius: 4px;
  `,
};

export default mixins;
